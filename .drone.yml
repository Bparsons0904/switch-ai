kind: pipeline
type: docker
name: default
steps:
  - name: deploy
    image: docker/compose:1.29.2
    environment:
      PORT:
        from_secret: PORT
    commands:
      - docker-compose down
      - docker-compose up --build
trigger:
  branch:
    - main
  event:
    - push
